{
  "name": "frontend",
  "version": "1.0.0",
  "proxy": "http://localhost:8080",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "dependencies": {
    "react-router-dom": "^7.10.1"
  }
}
